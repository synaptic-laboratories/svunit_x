---
phase: 02-history-aware-upstream-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-phase2-integration.sh
  - .planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json
  - .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md
autonomous: true
requirements:
  - SYNC-02
  - SYNC-03
must_haves:
  truths:
    - Maintainer can inspect a replayable Phase 2 baseline artifact and see the exact branch head, code-only fork head, upstream target commit, merge-base, and pre-merge anchor used for integration.
    - Maintainer can open a single decision ledger and see every `LCU-*` and inherited `HR-*` item seeded with an explicit planned resolution path before any merge is attempted.
    - The phase has a reusable shell validator before later plans depend on Phase 2 artifacts.
  artifacts:
    - path: tests/test-phase2-integration.sh
      provides: Replayable Phase 2 artifact and review validator.
    - path: .planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json
      provides: Machine-readable baseline and anchor manifest for the integration run.
    - path: .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md
      provides: Running ledger of per-LCU and per-HR decisions for the merge.
  key_links:
    - from: .planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json
      to: .planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json
      via: carried-forward upstream target, merge-base, and code-only fork head
    - from: .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md
      to: .planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
      via: seeded LCU rows and default dispositions
---

<objective>
Install the replayable scaffolding Phase 2 needs before any upstream merge work begins.

Purpose: The integration itself is not trustworthy unless the exact execution baseline, recovery anchor, and per-LCU decision ledger are frozen before the code changes start.
Output: A Phase 2 validator, a machine-readable integration baseline manifest, and a seeded decision ledger that Phase 2 updates as merge decisions are made.
</objective>

<execution_context>
@/srv/share/repo/pub/com/github/synaptic-laboratories/svunit_x/.codex/get-shit-done/workflows/execute-plan.md
@/srv/share/repo/pub/com/github/synaptic-laboratories/svunit_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-history-aware-upstream-integration/02-RESEARCH.md
@.planning/phases/02-history-aware-upstream-integration/02-VALIDATION.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json
@.planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-executive-summary.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-human-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install replayable Phase 2 integration validation</name>
  <read_first>
tests/test-phase2-integration.sh (new file)
.planning/phases/02-history-aware-upstream-integration/02-VALIDATION.md
.planning/phases/02-history-aware-upstream-integration/02-RESEARCH.md
.planning/phases/01-fork-delta-baseline-intent-record/01-human-review.md
  </read_first>
  <files>tests/test-phase2-integration.sh</files>
  <action>Create `tests/test-phase2-integration.sh` as an executable POSIX shell helper with exactly three subcommands: `files`, `requirements`, and `review`. `files` must require `02-integration-baseline.json` and `02-decision-ledger.md`. `requirements` must verify that the final Phase 2 artifacts explicitly mention `XILX-03`, `SYNC-01`, `SYNC-02`, `SYNC-03`, and all `LCU-01` through `LCU-06`; it should read `02-integration-summary.md` if present and fall back to `02-decision-ledger.md` with a clear message if the summary does not exist yet. `review` must verify that `02-decision-ledger.md` contains seeded rows for `LCU-01` through `LCU-06` plus `HR-01` through `HR-04`, and if `02-human-review.md` exists it must also verify the fields `source_artifact`, `row_id_or_hash`, `why_human_review`, and `safe_default_until_quartus_signoff`. Fail fast on missing files and print the exact missing artifact or row identifier.</action>
  <acceptance_criteria>
`bash -n tests/test-phase2-integration.sh` exits 0.
`rg -n "files|requirements|review|LCU-01|HR-01|XILX-03|SYNC-03" tests/test-phase2-integration.sh` returns matches.
The script exits non-zero when `02-integration-baseline.json` or `02-decision-ledger.md` is missing.
  </acceptance_criteria>
  <verify>
    <automated>bash -n tests/test-phase2-integration.sh</automated>
  </verify>
  <done>The repo has a reusable shell validator for Phase 2 baseline, requirements coverage, and review-ledger completeness.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the Phase 2 execution baseline and pre-merge anchor</name>
  <read_first>
.planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json (new file)
.planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json
.planning/phases/02-history-aware-upstream-integration/02-RESEARCH.md
tests/test-phase2-integration.sh
  </read_first>
  <files>.planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json</files>
  <action>Create a local recovery branch named `phase2-pre-merge-anchor` at the current branch head if it does not already exist, then write `02-integration-baseline.json` with the execution-time values for `current_branch_name`, `current_branch_head`, `code_only_fork_head`, `upstream_target_commit`, `merge_base`, `pre_merge_anchor_branch`, `working_tree_clean_before_merge`, `phase1_matrix_path`, `phase1_human_review_path`, and `phase1_baseline_path`. Use the carried-forward values `c2cb87111cf93cbf0f3f485730d314dbad3cb858`, `8e70653e2cbfe3ebe154a863a46bf482ded4bc19`, and `84b88033590a1469a238be84d8526b25a9f29d10` as the expected code-only fork head, target, and merge-base, but compute the current branch head and cleanliness at execution time. If the worktree is dirty before the merge starts, stop after writing the JSON and leave `working_tree_clean_before_merge` set to `false` instead of forcing the merge.</action>
  <acceptance_criteria>
`jq -r '.current_branch_name, .current_branch_head, .code_only_fork_head, .upstream_target_commit, .merge_base, .pre_merge_anchor_branch, .working_tree_clean_before_merge' .planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json` prints non-empty values.
`jq -r '.code_only_fork_head, .upstream_target_commit, .merge_base' .planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json` matches `c2cb87111cf93cbf0f3f485730d314dbad3cb858`, `8e70653e2cbfe3ebe154a863a46bf482ded4bc19`, and `84b88033590a1469a238be84d8526b25a9f29d10`.
`bash tests/test-phase2-integration.sh files` exits 0 once this task and Task 3 are complete.
  </acceptance_criteria>
  <verify>
    <automated>jq -r '.code_only_fork_head, .upstream_target_commit, .merge_base' .planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json</automated>
  </verify>
  <done>The phase has a machine-readable baseline and a recoverable pre-merge anchor before code integration starts.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the integration decision ledger from Phase 1</name>
  <read_first>
.planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md (new file)
.planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
.planning/phases/01-fork-delta-baseline-intent-record/01-human-review.md
.planning/phases/01-fork-delta-baseline-intent-record/01-executive-summary.md
.planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json
tests/test-phase2-integration.sh
  </read_first>
  <files>.planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md</files>
  <action>Create `02-decision-ledger.md` as the running decision table for this phase. Include one row for each `LCU-01` through `LCU-06` and each inherited `HR-01` through `HR-04`. Use exact columns for `decision_id`, `source_type`, `source_ref`, `paths`, `upstream_refs`, `planned_resolution`, `execution_notes`, `final_disposition`, and `review_status`. Seed the initial `planned_resolution` values from Phase 1: `LCU-02` -> `adopt upstream`, `LCU-03` -> `rewrite on top of upstream`, `LCU-04` -> `rewrite on top of upstream`, `LCU-05` -> `keep unless upstream-equivalent replacement is proven`, `LCU-06` -> `justified replacement required`, and `HR-01` through `HR-04` -> `carry forward unresolved`. Leave `final_disposition` blank until Plan 02 and Plan 03 update it.</action>
  <acceptance_criteria>
`rg -n "decision_id|source_type|source_ref|planned_resolution|final_disposition|review_status|LCU-01|LCU-06|HR-01|HR-04" .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md` returns matches.
`bash tests/test-phase2-integration.sh files` exits 0.
`bash tests/test-phase2-integration.sh review` exits 0 using the seeded ledger rows even before `02-human-review.md` exists.
  </acceptance_criteria>
  <verify>
    <automated>bash tests/test-phase2-integration.sh files && bash tests/test-phase2-integration.sh review</automated>
  </verify>
  <done>The phase has a seeded, file-backed decision ledger that execution updates instead of improvising merge choices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 1 artifacts -> Phase 2 merge setup | Phase 2 depends on Phase 1 hashes, LCU classifications, and human-review defaults being carried forward exactly |
| local git state -> integration baseline manifest | The phase must not start merge work from an unrecorded or dirty local state |
| decision ledger -> later code reconciliation | Plan 02 and Plan 03 will rely on the seeded ledger rather than re-deriving intent mid-merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | Phase 2 validator | mitigate | Create `tests/test-phase2-integration.sh` before other tasks depend on Phase 2 artifacts |
| T-02-02 | R | pre-merge recovery point | mitigate | Record the current head and anchor branch in `02-integration-baseline.json` before any merge is started |
| T-02-03 | T | seeded decision intent | mitigate | Pre-populate `02-decision-ledger.md` from the Phase 1 matrix and human-review file instead of reconstructing intent later |
| T-02-04 | E | dirty worktree merge start | mitigate | Record `working_tree_clean_before_merge` and stop before merging if the worktree is not clean |
</threat_model>

<verification>
Run `bash -n tests/test-phase2-integration.sh`, then run `bash tests/test-phase2-integration.sh files && bash tests/test-phase2-integration.sh review`.
</verification>

<success_criteria>
`tests/test-phase2-integration.sh`, `02-integration-baseline.json`, and `02-decision-ledger.md` exist and agree on the current execution baseline plus seeded `LCU-*` / `HR-*` decisions before any merge is attempted.
</success_criteria>

<output>
After completion, create `.planning/phases/02-history-aware-upstream-integration/02-01-SUMMARY.md`
</output>
