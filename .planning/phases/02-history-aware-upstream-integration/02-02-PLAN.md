---
phase: 02-history-aware-upstream-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - bin/runSVUnit
  - bin/cleanSVUnit
  - svunit_base/svunit_pkg.sv
  - svunit_base/svunit_testcase.sv
  - svunit_base/svunit_testsuite.sv
  - svunit_base/svunit_testrunner.sv
  - svunit_base/junit-xml/XmlElement.svh
  - svunit_base/svunit_test.svh
  - svunit_base/svunit_globals.svh
  - src/experimental/sv/svunit.sv
  - src/experimental/sv/svunit/test.svh
  - src/experimental/sv/svunit/testcase.svh
  - src/experimental/sv/svunit/testsuite.svh
  - src/testExperimental/sv/test_registry_unit_test.sv
  - test/test_list_tests.py
  - test/utils.py
  - .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md
autonomous: true
requirements:
  - XILX-03
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - Maintainer can inspect the synchronized code and see upstream `v3.38.1` behavior integrated without silently dropping required local Xilinx/Vivado behavior.
    - Every non-trivial conflict resolution can be traced back to a specific `LCU-*` or `HR-*` row rather than an unexplained text merge.
    - The integrated tree contains the expected upstream CLI/runtime/experimental surfaces and no unresolved merge conflicts.
  artifacts:
    - path: .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md
      provides: Running record of the exact keep/rewrite/supersede/replacement decisions made during code integration.
    - path: bin/runSVUnit
      provides: Final CLI reconciliation between upstream `v3.38.1` and the local xsim flow.
    - path: test/utils.py
      provides: Explicit Phase 2 replacement or carry-forward outcome for the host-side simulator-discovery concern.
  key_links:
    - from: .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md
      to: .planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
      via: per-LCU final dispositions and execution notes
    - from: bin/runSVUnit
      to: .planning/phases/01-fork-delta-baseline-intent-record/01-human-review.md
      via: explicit handling of `HR-03` / `LCU-01`
---

<objective>
Perform the actual upstream integration and reconcile the overlapping code surfaces by logical change unit.

Purpose: Phase 2 only succeeds if the code itself reaches the upstream target surface while preserving or explicitly replacing the fork's required Xilinx/Vivado behavior.
Output: Integrated CLI, runtime, experimental, helper-library, and host-side test surfaces plus an updated decision ledger that records what happened to each relevant local divergence.
</objective>

<execution_context>
@/srv/share/repo/pub/com/github/synaptic-laboratories/svunit_x/.codex/get-shit-done/workflows/execute-plan.md
@/srv/share/repo/pub/com/github/synaptic-laboratories/svunit_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-history-aware-upstream-integration/02-RESEARCH.md
@.planning/phases/02-history-aware-upstream-integration/02-VALIDATION.md
@.planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json
@.planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-human-review.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-executive-summary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge upstream `v3.38.1` and resolve the CLI / cleanup surface</name>
  <read_first>
bin/runSVUnit
bin/cleanSVUnit
.planning/phases/02-history-aware-upstream-integration/02-integration-baseline.json
.planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md
.planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
.planning/phases/01-fork-delta-baseline-intent-record/01-human-review.md
  </read_first>
  <files>bin/runSVUnit, bin/cleanSVUnit, .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md</files>
  <action>From a clean worktree confirmed by `02-integration-baseline.json`, start the upstream integration with `git merge --no-commit --no-ff 8e70653e2cbfe3ebe154a863a46bf482ded4bc19` if the merge has not already begun. Resolve `bin/runSVUnit` and `bin/cleanSVUnit` by preserving the upstream `v3.38.1` CLI surface: keep `-e|--e_arg`, keep `--list-tests`, keep the upstream unknown-simulator message path, and keep upstream xsim help text instead of the local `LCU-02` wording. Then reintroduce only the still-required local Vivado runtime details from `LCU-01`: the xsim compile/elaboration flow with `xvlog --relax` and `xelab --debug all --relax --override_timeunit --timescale 1ns/1ps` if they are still needed, plus the local cleanup entries for `xsim.dir`, `xsim*.*`, `xelab*.*`, `xvlog.pb`, `tests.xml`, and `work.testrunner.wdb`. Update the `LCU-01` and `LCU-02` rows in `02-decision-ledger.md` with exact execution notes and final dispositions.</action>
  <acceptance_criteria>
`git diff --name-only --diff-filter=U -- bin/runSVUnit bin/cleanSVUnit` outputs nothing.
`perl -c bin/runSVUnit && perl -c bin/cleanSVUnit` exits 0.
`rg -n -- "--list-tests|-e\\|--e_arg|Unknown simulator|xsim" bin/runSVUnit` returns matches.
`rg -n "xsim\\.dir|xsim\\*\\.\\*|xelab\\*\\.\\*|xvlog\\.pb|tests\\.xml|work\\.testrunner\\.wdb" bin/cleanSVUnit` returns matches unless a removal is explicitly justified in `02-decision-ledger.md`.
  </acceptance_criteria>
  <verify>
    <automated>git diff --name-only --diff-filter=U -- bin/runSVUnit bin/cleanSVUnit && perl -c bin/runSVUnit && perl -c bin/cleanSVUnit</automated>
  </verify>
  <done>The upstream CLI surface is integrated, the local xsim runtime behavior is either preserved or explicitly replaced, and the decision ledger records the exact outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve stable-runtime and helper-library overlaps by intent</name>
  <read_first>
svunit_base/svunit_pkg.sv
svunit_base/svunit_testcase.sv
svunit_base/svunit_testsuite.sv
svunit_base/svunit_testrunner.sv
svunit_base/junit-xml/XmlElement.svh
svunit_base/svunit_test.svh
svunit_base/svunit_globals.svh
.planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md
.planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
  </read_first>
  <files>svunit_base/svunit_pkg.sv, svunit_base/svunit_testcase.sv, svunit_base/svunit_testsuite.sv, svunit_base/svunit_testrunner.sv, svunit_base/junit-xml/XmlElement.svh, svunit_base/svunit_test.svh, svunit_base/svunit_globals.svh, .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md</files>
  <action>Resolve the stable-runtime and helper-library overlap by taking the upstream `v3.38.1` control-flow refactors as the baseline and then manually porting the local parser-sensitive intent from `LCU-03` and `LCU-05`. Keep upstream list-tests and testcase execution changes, keep upstream XML escaping behavior, preserve any new upstream files such as `svunit_base/svunit_test.svh` and `svunit_base/svunit_globals.svh`, and reapply local parser-compatibility details only where they are still needed after inspection of the merged code. Do not replay the old hunks verbatim if the surrounding upstream code moved. Update the `LCU-03` and `LCU-05` rows in `02-decision-ledger.md` with the exact files, local intent kept, and any justified replacements.</action>
  <acceptance_criteria>
`git diff --name-only --diff-filter=U -- svunit_base` outputs nothing.
`test -f svunit_base/svunit_test.svh && test -f svunit_base/svunit_globals.svh` exits 0.
`rg -n "LCU-03|LCU-05" .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md` returns matches.
`rg -n "^(<<<<<<<|=======|>>>>>>>)" svunit_base` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>git diff --name-only --diff-filter=U -- svunit_base && test -f svunit_base/svunit_test.svh && test -f svunit_base/svunit_globals.svh</automated>
  </verify>
  <done>The stable runtime and helper-library surfaces are reconciled against upstream structure, and their final dispositions are recorded in the decision ledger.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve experimental-flow and host-side regression surfaces</name>
  <read_first>
src/experimental/sv/svunit.sv
src/experimental/sv/svunit/test.svh
src/experimental/sv/svunit/testcase.svh
src/experimental/sv/svunit/testsuite.svh
src/testExperimental/sv/test_registry_unit_test.sv
test/test_list_tests.py
test/utils.py
.planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md
.planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
.planning/phases/01-fork-delta-baseline-intent-record/01-human-review.md
  </read_first>
  <files>src/experimental/sv/svunit.sv, src/experimental/sv/svunit/test.svh, src/experimental/sv/svunit/testcase.svh, src/experimental/sv/svunit/testsuite.svh, src/testExperimental/sv/test_registry_unit_test.sv, test/test_list_tests.py, test/utils.py, .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md</files>
  <action>Resolve the experimental flow by adopting the upstream `v3.38.1` layout under `src/experimental/sv/svunit/` and then manually porting the local parser-safe intent from `LCU-04` onto that layout. Keep upstream experimental adapter and new file structure intact, keep the upstream-added regression `test/test_list_tests.py` if the merge brings it in, and update `src/testExperimental/sv/test_registry_unit_test.sv` to match the merged experimental API surface. Handle `LCU-06` explicitly: if `test/utils.py` still contains `simulators = [$]`, replace it with valid Python list initialization, keep the `shutil.which('xsim')` append path, and record the change as a justified replacement tied to `HR-04` instead of claiming literal preservation. Update the `LCU-04` and `LCU-06` rows in `02-decision-ledger.md` with the exact final disposition and rationale.</action>
  <acceptance_criteria>
`git diff --name-only --diff-filter=U -- src/experimental/sv src/testExperimental/sv test/test_list_tests.py test/utils.py` outputs nothing.
`test -f src/experimental/sv/svunit/test.svh && test -f test/test_list_tests.py` exits 0.
`rg -n "simulators = \\[\\]" test/utils.py` returns a match.
`rg -n "simulators = \\[\\$\\]" test/utils.py` returns no matches.
`rg -n "LCU-04|LCU-06|HR-04" .planning/phases/02-history-aware-upstream-integration/02-decision-ledger.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>git diff --name-only --diff-filter=U -- src/experimental/sv src/testExperimental/sv test/test_list_tests.py test/utils.py && test -f src/experimental/sv/svunit/test.svh && test -f test/test_list_tests.py && rg -n "simulators = \\[\\]" test/utils.py</automated>
  </verify>
  <done>The experimental flow, upstream-added regression surfaces, and host-side simulator discovery are integrated with explicit ledger-backed outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| current branch state -> upstream merge operation | The merge must start from the recorded Phase 2 anchor and target commit, not an ad hoc file transplant |
| Phase 1 LCU intent -> Phase 2 code edits | Local Xilinx/Vivado behavior may only be preserved or replaced through recorded intent, not guesswork |
| merged code -> later Quartus sign-off | Phase 3 will trust Phase 2's code and review artifacts when deciding what to validate in Quartus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | T | unresolved merge conflicts | mitigate | Every task verifies its path group has no remaining unmerged files and no conflict markers |
| T-02-06 | I | local Xilinx/Vivado behavior loss | mitigate | Update `02-decision-ledger.md` per `LCU-*` row while preserving or explicitly replacing material behavior |
| T-02-07 | E | broken host-side regression helper | mitigate | Replace `simulators = [$]` with a valid, explicitly documented outcome tied to `HR-04` |
| T-02-08 | R | text-only conflict picks | mitigate | Require each overlapping path group to cite the Phase 1 matrix and write final dispositions into the decision ledger |
</threat_model>

<verification>
Resolve all merge conflicts, then run `perl -c bin/runSVUnit && perl -c bin/cleanSVUnit` and the task-local conflict/grep checks.
</verification>

<success_criteria>
The code tree is merged to the upstream target surface with no unresolved conflicts, the known Xilinx/Vivado-sensitive surfaces are preserved or explicitly replaced, and `02-decision-ledger.md` records what happened to every material local divergence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-history-aware-upstream-integration/02-02-SUMMARY.md`
</output>
