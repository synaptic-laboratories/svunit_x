# Phase 01 Fork Delta Matrix

Derived from merge-base `84b88033590a1469a238be84d8526b25a9f29d10` against fork head `c2cb87111cf93cbf0f3f485730d314dbad3cb858`.

The large `8e7d8d35e68a2deb0923871de998b13782f5f5ec` patch is only split where `git show` exposes a clean subsystem thread: CLI xsim flow, stable runtime parser compatibility, experimental parser compatibility, helper-library parser adjustments, and host-side regression fallout. The follow-on help-text commit `c2cb87111cf93cbf0f3f485730d314dbad3cb858` stays separate because `range-diff` identifies a concrete upstream counterpart for that narrow change.

| logical_change_id | files_touched | commits | likely_purpose_or_interpretation | xilinx_relevance | classification | merge_handling_notes | evidence_refs |
| --- | --- | --- | --- | --- | --- | --- | --- |
| LCU-01 | `bin/runSVUnit`<br>`bin/cleanSVUnit` | `8e7d8d35e68a2deb0923871de998b13782f5f5ec` | Enable a usable local xsim execution path by adding Xilinx-specific `xvlog --relax` and `xelab --debug all --relax --override_timeunit --timescale 1ns/1ps` handling, plus cleanup for Vivado-generated artifacts. | direct Xilinx/Vivado simulator execution path | human-review | Do not cherry-pick blindly. Upstream changed the same CLI surface for elaboration arguments and simulator help, but the local debug and cleanup choices are still only evidenced in the fork. Re-test whether each flag or cleanup step is still required for Vivado-specific crashes before carrying it forward. | `evidence/fork-only.log` for the 8e7d patch contents; `evidence/path-overlap.txt` for `bin/runSVUnit`; `evidence/range-diff.txt` because the overall patch has no direct upstream twin |
| LCU-02 | `bin/runSVUnit` | `c2cb87111cf93cbf0f3f485730d314dbad3cb858` | Expose xsim in the `runSVUnit --help` simulator list after the local Xilinx execution path existed. | direct Xilinx/Vivado simulator help surface | superseded | Upstream already landed the same user-visible help intent in `93d3e7e`, and upstream also extended the usage line with the newer elaboration-argument option. Carry the upstream wording instead of replaying this fork commit. | `evidence/fork-only.log` for `c2cb87111cf93cbf0f3f485730d314dbad3cb858`; `evidence/path-overlap.txt` for `bin/runSVUnit`; `evidence/range-diff.txt` for the `c2cb871 ! 93d3e7e` match |
| LCU-03 | `svunit_base/svunit_base.sv`<br>`svunit_base/svunit_pkg.sv`<br>`svunit_base/svunit_testcase.sv`<br>`svunit_base/svunit_testrunner.sv`<br>`svunit_base/svunit_testsuite.sv`<br>`svunit_base/svunit_internal_defines.svh` | `8e7d8d35e68a2deb0923871de998b13782f5f5ec` | Make the stable runtime friendlier to the Xilinx parser by adding explicit `input` directions, switching static arrays to dynamic arrays `[$]`, and routing fatal handling through `__svunit_fatal` so simulator-specific `$fatal` behavior can be controlled in one place. | material Xilinx/Vivado parser and warning compatibility in stable runtime | rewrite | Re-apply this intent manually on top of upstream `v3.38.1`. The same files were heavily refactored upstream around test execution and testcase reporting, so replaying the old patch verbatim would collide with newer control flow and may miss the real current insertion points. | `evidence/fork-only.log` for the 8e7d file list; `evidence/path-overlap.txt` for overlapping stable-runtime files; `evidence/range-diff.txt` because the 8e7d patch is unmatched upstream |
| LCU-04 | `src/experimental/sv/svunit.sv`<br>`src/experimental/sv/test_registry.svh`<br>`src/experimental/sv/testcase.svh`<br>`src/experimental/sv/testsuite.svh`<br>`src/testExperimental/sv/test_registry_unit_test.sv` | `8e7d8d35e68a2deb0923871de998b13782f5f5ec` | Keep the experimental self-registering flow compiling under Xilinx by converting array declarations to dynamic arrays `[$]`, using parser-safe local declarations, and aligning the experimental regression with those return-type changes. | material Xilinx/Vivado parser compatibility in experimental flow | rewrite | Re-port by hand rather than cherry-picking. Upstream changed the same experimental files repeatedly after the merge-base, so the local parser-compat declarations need to be compared against the current upstream shapes and only retained where the modern code still needs them. | `evidence/fork-only.log` for the 8e7d experimental hunks; `evidence/path-overlap.txt` for `src/experimental/sv/svunit.sv` and `src/experimental/sv/testcase.svh`; `evidence/range-diff.txt` because the fork patch remains unmatched |
| LCU-05 | `svunit_base/junit-xml/TestCase.svh`<br>`svunit_base/junit-xml/TestSuite.svh`<br>`svunit_base/junit-xml/XmlElement.svh`<br>`svunit_base/junit-xml/junit_xml.sv`<br>`svunit_base/svunit_filter.svh`<br>`svunit_base/svunit_filter_for_single_pattern.svh`<br>`svunit_base/svunit_string_utils.svh`<br>`src/test/sv/string_utils_unit_test.sv` | `8e7d8d35e68a2deb0923871de998b13782f5f5ec` | Harden helper code that sits on Xilinx-sensitive parser paths by adding `input` qualifiers, converting helper arrays to `[$]`, and replacing direct fatal macros in filter and string utilities with the centralized wrapper. The paired `string_utils` test update documents the expected dynamic-array behavior. | indirect Xilinx/Vivado parser compatibility in helper libraries | keep | Keep this as a local logical unit unless later validation proves the upstream helper code already compiles cleanly under Vivado without these declarations. The visible upstream overlap here is an XML-escaping fix, not the same parser-compatibility intent. | `evidence/fork-only.log` for helper-library and test files; `evidence/path-overlap.txt` for `svunit_base/junit-xml/XmlElement.svh` only |
| LCU-06 | `test/utils.py` | `8e7d8d35e68a2deb0923871de998b13782f5f5ec` | Likely attempt to update host-side simulator discovery for the new Xilinx support, but the recorded edit `simulators = [$]` is not self-explanatory and may be an accidental broken line rather than a deliberate Python change. | Xilinx/Vivado-related host simulator discovery follow-on | human-review | Do not carry this line automatically. Inspect whether the intended outcome was to surface xsim in host-side regression detection and, if so, re-implement it explicitly in valid Python with a matching regression check instead of replaying the suspect line. | `evidence/fork-only.log` for the `test/utils.py` hunk; `evidence/range-diff.txt` because the parent 8e7d patch has no direct upstream counterpart |
