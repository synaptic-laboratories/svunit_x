---
phase: 01-fork-delta-baseline-intent-record
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/test-phase1-matrix.sh
  - .planning/phases/01-fork-delta-baseline-intent-record/evidence/fork-only.log
  - .planning/phases/01-fork-delta-baseline-intent-record/evidence/range-diff.txt
  - .planning/phases/01-fork-delta-baseline-intent-record/evidence/path-overlap.txt
  - .planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
autonomous: true
requirements:
  - BASE-02
  - BASE-03
  - XILX-01
  - XILX-02
must_haves:
  truths:
    - Maintainer can open one file-backed fork-vs-upstream comparison and see every logical fork-only change unit with a supported classification.
    - Maintainer can trace material Xilinx/Vivado-related behavior to the commits and files that introduced or adjusted it.
    - Maintainer can inspect the matrix rows themselves and see row-level intent and merge-handling notes for each logical change unit, especially Xilinx/Vivado-related ones.
  artifacts:
    - path: tests/test-phase1-matrix.sh
      provides: Replayable shell validation for the matrix and evidence set.
    - path: .planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
      provides: Master logical-change matrix with classifications and evidence references.
    - path: .planning/phases/01-fork-delta-baseline-intent-record/evidence/fork-only.log
      provides: Fork-only commit range and per-commit file evidence.
  key_links:
    - from: .planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
      to: .planning/phases/01-fork-delta-baseline-intent-record/evidence/range-diff.txt
      via: evidence reference for every `superseded`, `rewrite`, or `human-review` decision
    - from: tests/test-phase1-matrix.sh
      to: .planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md
      via: column, classification, Xilinx-trace, and row-level intent checks
---

<objective>
Build the reusable fork-delta evidence set and the master logical-change matrix from the verified merge-base.

Purpose: Phase 2 needs a file-backed map of local change units, their upstream overlap, and their risk classification before any merge or cherry-pick choice is made.
Output: A matrix verifier, raw fork-only/range-diff/path-overlap evidence, and the master fork-delta matrix.
</objective>

<execution_context>
@/srv/share/repo/pub/com/github/synaptic-laboratories/svunit_x/.codex/get-shit-done/workflows/execute-plan.md
@/srv/share/repo/pub/com/github/synaptic-laboratories/svunit_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-CONTEXT.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-RESEARCH.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-VALIDATION.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json
@.planning/phases/01-fork-delta-baseline-intent-record/01-baseline-review.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install replayable matrix verification checks</name>
  <read_first>
tests/test-phase1-matrix.sh (new file)
.planning/phases/01-fork-delta-baseline-intent-record/01-VALIDATION.md
.planning/phases/01-fork-delta-baseline-intent-record/01-CONTEXT.md
.planning/phases/01-fork-delta-baseline-intent-record/01-RESEARCH.md
  </read_first>
  <files>tests/test-phase1-matrix.sh</files>
  <action>Create `tests/test-phase1-matrix.sh` as an executable POSIX shell helper with exactly four subcommands: `files`, `classifications`, `xilinx-trace`, and `intent`. `files` must verify the presence of `01-fork-delta-matrix.md` and the three evidence files named in this plan. `classifications` must assert that every matrix row uses one of `keep`, `superseded`, `rewrite`, or `human-review` and that the matrix contains the D-16 columns `logical_change_id`, `files_touched`, `commits`, `likely_purpose_or_interpretation`, `xilinx_relevance`, `classification`, `merge_handling_notes`, and `evidence_refs`. `xilinx-trace` must verify that the matrix rows touching Xilinx/Vivado-related behavior name both the commit and at least one touched file. `intent` is the automated XILX-02 gate and must inspect `01-fork-delta-matrix.md` itself: verify that every non-header logical-change row has non-empty `likely_purpose_or_interpretation` and `merge_handling_notes`, and that rows marked Xilinx/Vivado-relevant do not leave those cells blank or defer them to `01-executive-summary.md` or `01-human-review.md`. Fail fast on missing files and print the row ID that violated a check.</action>
  <acceptance_criteria>
`bash -n tests/test-phase1-matrix.sh` exits 0.
`rg -n "files|classifications|xilinx-trace|intent" tests/test-phase1-matrix.sh` returns four subcommand matches.
The script uses the exact allowed classification set `keep|superseded|rewrite|human-review`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n tests/test-phase1-matrix.sh</automated>
  </verify>
  <done>The repo has a reusable validator for the Phase 1 matrix, classifications, Xilinx traceability, and row-level intent/merge-handling notes in the matrix itself.</done>
</task>

<task type="auto">
  <name>Task 2: Capture fork-only commit, overlap, and counterpart evidence</name>
  <read_first>
.planning/phases/01-fork-delta-baseline-intent-record/evidence/fork-only.log (new file)
.planning/phases/01-fork-delta-baseline-intent-record/evidence/range-diff.txt (new file)
.planning/phases/01-fork-delta-baseline-intent-record/evidence/path-overlap.txt (new file)
.planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json
.planning/phases/01-fork-delta-baseline-intent-record/01-RESEARCH.md
tests/test-phase1-matrix.sh
  </read_first>
  <files>.planning/phases/01-fork-delta-baseline-intent-record/evidence/fork-only.log, .planning/phases/01-fork-delta-baseline-intent-record/evidence/range-diff.txt, .planning/phases/01-fork-delta-baseline-intent-record/evidence/path-overlap.txt</files>
  <action>Using the verified merge-base from `01-upstream-baseline.json`, save the fork-only evidence required by D-09 through D-13. In `evidence/fork-only.log`, record `git rev-list --reverse ${merge_base}..c2cb87111cf93cbf0f3f485730d314dbad3cb858` and `git show --stat --name-only` output for each fork-only commit, explicitly including `8e7d8d35e68a2deb0923871de998b13782f5f5ec` and `c2cb87111cf93cbf0f3f485730d314dbad3cb858`. In `evidence/range-diff.txt`, save `git range-diff ${merge_base}..c2cb87111cf93cbf0f3f485730d314dbad3cb858 ${merge_base}..8e70653e2cbfe3ebe154a863a46bf482ded4bc19` verbatim as human evidence only; do not post-process it mechanically. In `evidence/path-overlap.txt`, save the output of `comm -12 <(git diff --name-only ${merge_base}..fork | sort) <(git diff --name-only ${merge_base}..target | sort)` so the overlap surface is explicit. Preserve the exact command lines inside each evidence file so later reviewers can replay them.</action>
  <acceptance_criteria>
`rg -n "8e7d8d35e68a2deb0923871de998b13782f5f5ec|c2cb87111cf93cbf0f3f485730d314dbad3cb858" .planning/phases/01-fork-delta-baseline-intent-record/evidence/fork-only.log` returns matches.
`rg -n "range-diff|93d3e7e|!:" .planning/phases/01-fork-delta-baseline-intent-record/evidence/range-diff.txt` returns evidence that the upstream counterpart review was captured.
`wc -l .planning/phases/01-fork-delta-baseline-intent-record/evidence/path-overlap.txt` reports at least one overlapping path.
  </acceptance_criteria>
  <verify>
    <automated>bash tests/test-phase1-matrix.sh files</automated>
  </verify>
  <done>The phase directory contains replayable raw evidence for fork-only commits, upstream counterpart comparison, and path overlap from the true fork point.</done>
</task>

<task type="auto">
  <name>Task 3: Build the master logical-change matrix</name>
  <read_first>
.planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md (new file)
.planning/phases/01-fork-delta-baseline-intent-record/evidence/fork-only.log
.planning/phases/01-fork-delta-baseline-intent-record/evidence/range-diff.txt
.planning/phases/01-fork-delta-baseline-intent-record/evidence/path-overlap.txt
.planning/phases/01-fork-delta-baseline-intent-record/01-CONTEXT.md
.planning/phases/01-fork-delta-baseline-intent-record/01-RESEARCH.md
tests/test-phase1-matrix.sh
  </read_first>
  <files>.planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md</files>
  <action>Create the primary Phase 1 artifact `01-fork-delta-matrix.md` as one master Markdown table per D-14, D-15, and D-16. Use one row per logical change unit, not one row per file and not one row per commit unless they are the same logical unit. Start from the verified merge-base, keep `8e7d8d35e68a2deb0923871de998b13782f5f5ec` as a single reviewable logical unit unless the evidence proves a clean split, and include `c2cb87111cf93cbf0f3f485730d314dbad3cb858` because it affects the same simulator-facing behavior thread per D-12 and D-13. The table must include exact columns for `logical_change_id`, `files_touched`, `commits`, `likely_purpose_or_interpretation`, `xilinx_relevance`, `classification`, `merge_handling_notes`, and `evidence_refs`. Populate `likely_purpose_or_interpretation` and `merge_handling_notes` for every row in this matrix; do not defer those fields to Plan 03. Apply D-10 by treating parser-facing, static-casting, formal-semantics, and warning-reduction edits in Xilinx-affected areas as material by default. Apply D-18 through D-21 by marking unclear intent, unclear upstream subsumption, or parser-sensitive uncertainty as `human-review` rather than guessing. Do not use `superseded` unless the row cites concrete upstream evidence from `range-diff.txt` or a path-specific upstream log.</action>
  <acceptance_criteria>
`rg -n "logical_change_id|files_touched|commits|likely_purpose_or_interpretation|xilinx_relevance|classification|merge_handling_notes|evidence_refs" .planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md` returns matches.
`rg -n "LCU-01|8e7d8d35e68a2deb0923871de998b13782f5f5ec|c2cb87111cf93cbf0f3f485730d314dbad3cb858|human-review|keep|rewrite|superseded" .planning/phases/01-fork-delta-baseline-intent-record/01-fork-delta-matrix.md` returns matches.
Each Xilinx- or Vivado-relevant row names at least one file under `bin/`, `svunit_base/`, `src/experimental/sv/`, `test/`, or repo docs.
  </acceptance_criteria>
  <verify>
    <automated>bash tests/test-phase1-matrix.sh classifications && bash tests/test-phase1-matrix.sh xilinx-trace && bash tests/test-phase1-matrix.sh intent</automated>
  </verify>
  <done>The repo contains a single reviewable matrix that classifies every fork-only logical change unit, ties material Xilinx/Vivado behavior back to commits and files, and records row-level intent plus merge-handling notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline manifest -> delta evidence commands | Plan 02 trusts the merge-base and target hashes established in Plan 01 |
| raw git evidence -> human classification | Reviewers will use `range-diff`, path overlap, and file-touch evidence to classify change units |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | fork-only evidence files | mitigate | Save raw command output with the exact replay command in each evidence file and verify file presence with `tests/test-phase1-matrix.sh files` |
| T-01-06 | I | parser- or warning-sensitive edits | mitigate | Apply D-10 explicitly in the matrix and default unclear parser-facing or warning-reduction edits to material, not cosmetic |
| T-01-07 | R | classification rationale | mitigate | Require every matrix row to cite `evidence_refs` and forbid unexplained `superseded` classifications |
| T-01-08 | E | unsafe automation from `range-diff` | mitigate | Treat `range-diff` as human evidence only and record explicit row-level notes instead of parsing its porcelain output mechanically |
</threat_model>

<verification>
Run `bash tests/test-phase1-matrix.sh files`, `bash tests/test-phase1-matrix.sh classifications`, `bash tests/test-phase1-matrix.sh xilinx-trace`, and `bash tests/test-phase1-matrix.sh intent`.
</verification>

<success_criteria>
`01-fork-delta-matrix.md` exists with one row per logical change unit, valid classifications, evidence references, and row-level intent plus merge-handling notes. The three raw evidence files exist and let a maintainer replay the fork-only range and counterpart analysis from the verified merge-base.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-delta-baseline-intent-record/01-02-SUMMARY.md`
</output>
