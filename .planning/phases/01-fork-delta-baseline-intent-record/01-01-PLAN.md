---
phase: 01-fork-delta-baseline-intent-record
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test-phase1-baseline.sh
  - .planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json
  - .planning/phases/01-fork-delta-baseline-intent-record/evidence/refs.txt
  - .planning/phases/01-fork-delta-baseline-intent-record/evidence/merge-base.txt
  - .planning/phases/01-fork-delta-baseline-intent-record/01-baseline-review.md
autonomous: true
requirements:
  - BASE-01
must_haves:
  truths:
    - Maintainer can open a repo artifact and see the authoritative upstream URL, target tag, resolved target commit, remembered baseline tag, derived merge-base, and candidate-marker interpretation for this sync round.
    - Maintainer can see whether the remembered baseline and candidate marker agree with the computed graph, and any disagreement is explicitly classified as human-review instead of silently resolved.
  artifacts:
    - path: tests/test-phase1-baseline.sh
      provides: Replayable shell validation for the pinned baseline artifacts.
    - path: .planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json
      provides: Machine-readable baseline manifest with exact hashes and discrepancy flags.
    - path: .planning/phases/01-fork-delta-baseline-intent-record/01-baseline-review.md
      provides: Human-review note for ancestry or target-resolution ambiguity.
  key_links:
    - from: tests/test-phase1-baseline.sh
      to: .planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json
      via: jq/grep assertions on recorded ref and graph fields
    - from: .planning/phases/01-fork-delta-baseline-intent-record/01-baseline-review.md
      to: .planning/phases/01-fork-delta-baseline-intent-record/evidence/merge-base.txt
      via: exact hash references for every discrepancy
---

<objective>
Pin the authoritative upstream baseline for Phase 1 and record graph discrepancies in reviewable repo artifacts before any fork-delta classification begins.

Purpose: Phase 2 cannot safely reason about fork-only changes unless the upstream target, remembered baseline, derived merge-base, and candidate-marker semantics are verified from Git and written down exactly once.
Output: A replayable baseline test script, a pinned JSON baseline manifest, raw ref and merge-base evidence, and a human-review note for any graph mismatch.
</objective>

<execution_context>
@/srv/share/repo/pub/com/github/synaptic-laboratories/svunit_x/.codex/get-shit-done/workflows/execute-plan.md
@/srv/share/repo/pub/com/github/synaptic-laboratories/svunit_x/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-CONTEXT.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-RESEARCH.md
@.planning/phases/01-fork-delta-baseline-intent-record/01-VALIDATION.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install replayable baseline verification checks</name>
  <read_first>
tests/test-phase1-baseline.sh (new file)
.planning/phases/01-fork-delta-baseline-intent-record/01-VALIDATION.md
.planning/phases/01-fork-delta-baseline-intent-record/01-CONTEXT.md
.planning/phases/01-fork-delta-baseline-intent-record/01-RESEARCH.md
  </read_first>
  <files>tests/test-phase1-baseline.sh</files>
  <action>Create `tests/test-phase1-baseline.sh` as an executable POSIX shell helper with exactly two subcommands: `refs` and `graph`. `refs` must validate the baseline manifest fields required by D-01, D-02, D-03, and D-04 against the raw evidence files. `graph` must validate D-05, D-06, D-07, and D-08 by checking the remembered `v3.37.0` baseline, candidate marker `dc7ed0a5a8b88533b52d884e2c473beb9d4ce273`, derived merge-base, and the review-note disposition. Use `jq` for JSON field checks, fail fast on missing files, and print actionable error text naming the missing field or mismatched hash. The script must expect the 2026-04-11 research values as the comparison target for this run: target tag `v3.38.1`, target commit `8e70653e2cbfe3ebe154a863a46bf482ded4bc19`, remembered baseline commit `355c1411baf4d0233cb7862e53873ae90ec807e5`, derived merge-base `84b88033590a1469a238be84d8526b25a9f29d10`, candidate marker `dc7ed0a5a8b88533b52d884e2c473beb9d4ce273`, and first-parent first descendant `6e179cadaa036554452f8e82e9ca9e94bf307c40`; if execution-time Git output differs, the later tasks must update the manifest and review note rather than masking the discrepancy.</action>
  <acceptance_criteria>
`bash -n tests/test-phase1-baseline.sh` exits 0.
`rg -n "refs|graph|v3\\.38\\.1|dc7ed0a5a8b88533b52d884e2c473beb9d4ce273" tests/test-phase1-baseline.sh` returns matches.
The script exits non-zero when a required artifact path is missing.
  </acceptance_criteria>
  <verify>
    <automated>bash -n tests/test-phase1-baseline.sh</automated>
  </verify>
  <done>The repo has a reusable shell validator for Phase 1 baseline artifacts, and it encodes the exact fields and hashes Phase 1 must prove or flag.</done>
</task>

<task type="auto">
  <name>Task 2: Record the authoritative upstream target and computed graph</name>
  <read_first>
.planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json (new file)
.planning/phases/01-fork-delta-baseline-intent-record/evidence/refs.txt (new file)
.planning/phases/01-fork-delta-baseline-intent-record/evidence/merge-base.txt (new file)
.planning/phases/01-fork-delta-baseline-intent-record/01-CONTEXT.md
.planning/phases/01-fork-delta-baseline-intent-record/01-RESEARCH.md
tests/test-phase1-baseline.sh
  </read_first>
  <files>.planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json, .planning/phases/01-fork-delta-baseline-intent-record/evidence/refs.txt, .planning/phases/01-fork-delta-baseline-intent-record/evidence/merge-base.txt</files>
  <action>Re-run the authoritative Git commands from research and save both raw evidence and the pinned manifest. Query `https://github.com/svunit/svunit.git` per D-01 with `git ls-remote --tags` for `v3.38.1*` and `v3.37.0*`, and save the raw output to `evidence/refs.txt`. Recompute the graph using fork head `c2cb87111cf93cbf0f3f485730d314dbad3cb858`, target commit `8e70653e2cbfe3ebe154a863a46bf482ded4bc19`, remembered baseline commit `355c1411baf4d0233cb7862e53873ae90ec807e5`, and candidate marker `dc7ed0a5a8b88533b52d884e2c473beb9d4ce273`; save `git merge-base`, `git merge-base --is-ancestor`, `git rev-list --reverse`, and `git rev-list --first-parent --reverse` output to `evidence/merge-base.txt`. Write `01-upstream-baseline.json` with concrete keys for `upstream_url`, `target_tag`, `target_tag_object`, `target_commit`, `remembered_baseline_tag`, `remembered_baseline_commit`, `fork_head`, `derived_merge_base`, `candidate_marker`, `candidate_marker_full_ancestry_first_descendant`, `candidate_marker_first_parent_first_descendant`, `remembered_baseline_matches_merge_base`, `candidate_marker_matches_full_ancestry_first_descendant`, `candidate_marker_matches_first_parent_first_descendant`, and `baseline_disposition`. Do not hardcode the final values without recomputing them; the manifest must reflect execution-time results and preserve D-02 through D-06 exactly.</action>
  <acceptance_criteria>
`jq -r '.upstream_url, .target_tag, .target_commit, .derived_merge_base' .planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json` prints non-empty values.
`rg -n "refs/tags/v3\\.38\\.1|refs/tags/v3\\.37\\.0" .planning/phases/01-fork-delta-baseline-intent-record/evidence/refs.txt` returns matches.
`rg -n "84b88033590a1469a238be84d8526b25a9f29d10|dc7ed0a5a8b88533b52d884e2c473beb9d4ce273|6e179cadaa036554452f8e82e9ca9e94bf307c40" .planning/phases/01-fork-delta-baseline-intent-record/evidence/merge-base.txt` returns matches unless execution-time recomputation changed them, in which case the file must contain the new values and the review note must explain the mismatch.
  </acceptance_criteria>
  <verify>
    <automated>bash tests/test-phase1-baseline.sh refs</automated>
  </verify>
  <done>The phase directory contains a machine-readable baseline manifest and raw Git evidence proving the authoritative upstream target and computed fork graph.</done>
</task>

<task type="auto">
  <name>Task 3: Classify baseline and marker mismatches as human-review</name>
  <read_first>
.planning/phases/01-fork-delta-baseline-intent-record/01-baseline-review.md (new file)
.planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json
.planning/phases/01-fork-delta-baseline-intent-record/evidence/merge-base.txt
.planning/phases/01-fork-delta-baseline-intent-record/01-CONTEXT.md
  </read_first>
  <files>.planning/phases/01-fork-delta-baseline-intent-record/01-baseline-review.md</files>
  <action>Write `01-baseline-review.md` as the explicit discrepancy record for D-07 and D-08. The note must name the remembered baseline `v3.37.0`, the remembered baseline commit, the derived merge-base, the candidate marker `dc7ed0a5a8b88533b52d884e2c473beb9d4ce273`, and the first-parent first descendant `6e179cadaa036554452f8e82e9ca9e94bf307c40`, then state whether each mismatch is `human-review` or resolved. If `git ls-remote` cannot resolve a single clean `v3.38.1^{}` target commit, write that as a blocking item and stop the plan with a non-zero exit after creating the note. If the target resolves cleanly but the remembered baseline or marker semantics disagree with the computed graph, record that discrepancy as `human-review`, explain why the phase can still continue, and explicitly prohibit silent auto-resolution in later phases.</action>
  <acceptance_criteria>
`rg -n "human-review|v3\\.37\\.0|dc7ed0a5a8b88533b52d884e2c473beb9d4ce273|6e179cadaa036554452f8e82e9ca9e94bf307c40|84b88033590a1469a238be84d8526b25a9f29d10" .planning/phases/01-fork-delta-baseline-intent-record/01-baseline-review.md` returns matches.
If `jq -r '.baseline_disposition' .planning/phases/01-fork-delta-baseline-intent-record/01-upstream-baseline.json` is `human-review`, the review note must explain why.
  </acceptance_criteria>
  <verify>
    <automated>bash tests/test-phase1-baseline.sh graph</automated>
  </verify>
  <done>The phase has a durable human-review artifact for every ancestry disagreement, and ambiguous target resolution blocks execution instead of being guessed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> upstream Git refs | Remote ref data from `https://github.com/svunit/svunit.git` is authoritative and must not be substituted with fork-local refs |
| shell verifier -> repo artifacts | Verification scripts read JSON and evidence files that later plans will trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | upstream ref resolution | mitigate | Use the explicit upstream URL from D-01, save raw `git ls-remote` output in `evidence/refs.txt`, and fail if `v3.38.1^{}` does not resolve uniquely |
| T-01-02 | T | baseline manifest integrity | mitigate | Make `tests/test-phase1-baseline.sh` compare `01-upstream-baseline.json` against raw evidence files and fail on missing or mismatched hashes |
| T-01-03 | R | ancestry discrepancy handling | mitigate | Write every baseline and marker mismatch into `01-baseline-review.md` with exact hashes so later phases cannot claim the discrepancy was never observed |
| T-01-04 | E | ambiguous target acceptance | mitigate | Treat an unresolved or multiply-resolved `v3.38.1` target as blocking per D-08 and exit non-zero after writing the review note |
</threat_model>

<verification>
Run `bash tests/test-phase1-baseline.sh refs` and `bash tests/test-phase1-baseline.sh graph`.
</verification>

<success_criteria>
`01-upstream-baseline.json`, `evidence/refs.txt`, `evidence/merge-base.txt`, and `01-baseline-review.md` exist and agree with the verifier. The plan either proves a clean `v3.38.1` target and records baseline ambiguity as `human-review`, or it blocks with a written discrepancy note.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-delta-baseline-intent-record/01-01-SUMMARY.md`
</output>
